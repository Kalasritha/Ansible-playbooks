Ansible Playbook – Install Docker, Pull Image & Run Container

This project contains an Ansible playbook that automates the installation of Docker on an Ubuntu server, pulls a Docker image, and starts a container automatically.
